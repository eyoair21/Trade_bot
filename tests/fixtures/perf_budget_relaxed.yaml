# Relaxed performance budget for testing pass scenarios
metric: sharpe
mode: max

# Very relaxed thresholds that should pass
min_success_rate: 0.5
max_p90_elapsed_s: 120.0
max_sharpe_drop: 1.0
epsilon_abs: 0.1
