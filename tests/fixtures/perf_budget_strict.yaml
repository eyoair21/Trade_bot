# Strict performance budget for testing failure scenarios
metric: sharpe
mode: max

# Very strict thresholds that will likely fail
min_success_rate: 0.99
max_p90_elapsed_s: 5.0
max_sharpe_drop: 0.01
epsilon_abs: 1e-9
