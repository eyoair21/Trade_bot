name: liquid_top1000
description: >
  Market-wide liquid universe targeting the top ~1000 names by
  dollar volume, subject to basic price, spread, and history screens.

base:
  symbols: []  # All available symbols are candidates by default

screens:
  min_price: 5.0
  min_adv: 20000000.0
  max_spread_pct: 0.002
  min_history_days: 756
  max_symbols: 1000


